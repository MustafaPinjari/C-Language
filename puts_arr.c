#include<stdio.h>
#include<conio.h>
    void main()
    {
        char str[30];
            printf("enter a string: ");
            fgets(str,stdin);
                printf("the string is:");
                puts(str);
                
    }