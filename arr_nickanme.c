#include<stdio.h>
#include<conio.h>
#include<string.h>
    void main()
        {
            char str[20];
            puts("enter ur nick name=");
                gets(str);
                    puts(str);
                    getch();
        }