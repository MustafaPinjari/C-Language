#include<stdio.h>
#include<conio.h>
#include<string.h>
    void main()
        {
            char str[20];
                 printf("enter a string in lower case =");
                 gets(str);

                        printf("\n upper string  is=%s",strupr(str));
        }